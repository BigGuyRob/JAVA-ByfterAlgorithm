/**
 * Driver Class contains logic for accessing and manipulating driver fields
 * @author Robert Reid 
 */
public class Driver extends Person{

	/**
	 * Default constructor for the Driver class is the same as the person
	 * @param start - Location driver is starting from
	 * @param type - String type of Person ie "driver"
	 */
	public Driver(Location start, String type) {
		super(start, type);
	}
	
	
}
